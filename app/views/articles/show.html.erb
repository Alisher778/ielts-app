<div class="row">
  <div class="col col-md-12">
    <h2 align="center"><%= @article.title %></h2>
    <%= image_tag @article.photo.url(:large), class: "thumb" %>
    <div><%= @article.contant.html_safe %></div>
  </div>
</div>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
